<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC 	
	"-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blockAuth.mapper.LoginMapper">
<select id="loginSelect" parameterType="string" resultType="authInfo">
	select buyer_id userId, buyer_pw userPw, 'buyer' grade
	from buyer
	where buyer_id = #{buyerId}
	union
	select seller_id userId, seller_pw userPw, 'seller'
	from seller
	where seller_id = #{sellerId}
	union
	select admin_id userId, admin_pw userPw, 'admin'
	from admin1
	where admin_id = #{adminId}
</select>
</mapper>